package No005_LongestPalindromicSubstring

object LongestPalindromicSubstring {
  def longestPalindrome(s: String): String = {
    def findPalindrome(chars: List[Char]): (Boolean, List[Char]) = {
      val isPalindrome,
      if (isPalindrome(chars.init.tail)._1) {
        if (chars.head == chars.last) {
          true
        } else {

        }
      }
    }
  }
}
